# Web framework
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
click==8.1.8
itsdangerous==2.2.0
MarkupSafe==3.0.3
blinker==1.9.0

# Data
pandas==2.2.3
numpy==1.26.4
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3

# ML / NLP
torch==2.2.2. # For CPU-only, use torch==2.2.2+cpu - see https://pytorch.org/get-started/locally/
# use transformers 4.39.3 for better compatibility with older PyTorch versions and to avoid issues with newer versions
# pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
transformers==4.39.3
tqdm==4.67.1

# PDF export
reportlab

# HTTP / scraping (FedDocumentFetcher)
requests
beautifulsoup4

# Production server
gunicorn


# For development, you can also install the following (not required for production):
# For development server with auto-reload
# pip install flask[dev]